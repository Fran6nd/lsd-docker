# spicyctf instance
LSD_PORT=32889
LSD_NAME=Fran6nd's CTF with spicy guns
LSD_GAMEMODE=ctf
# rotation follows instances/spicyctf.map/ -- uncomment to pin the order
#LSD_MAPS=hallway Penthouse Prauda Remus
LSD_CONFIG_FILE=./instances/spicyctf.config.lua
LSD_RW_VOLUME=lsd-spicyctf-rw
# run the in-development scripts.dev/ layer (0 = released scripts only)
LSD_SCRIPTS_DEV=1
# announce this instance to the public server lists (0 = unlisted)
LSD_MASTERLIST=1
# this server's own maps; nothing else reads or writes them
LSD_MAPS_DIR=./instances/spicyctf.map
